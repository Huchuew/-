# 레거시 Phaser 포커 코드 (아카이브)

투닥투닥 RPG v2는 `src/tuduk/` + Canvas 기반입니다.  
이 디렉터리의 파일은 초기 Phaser 포커 프로토타입 잔재로, **빌드·런타임에 사용되지 않습니다**.

- `tsconfig.json`에서 `exclude` 처리됨
- 진입점: `src/main.ts` → `bootTudukRPG()` only

삭제 전 참고용으로 보관하거나, 필요 시 이 README만 남기고 폴더를 제거해도 됩니다.
